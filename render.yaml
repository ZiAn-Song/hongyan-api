services:
  - type: web
    name: hongyan-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn FileApi:app --bind 0.0.0.0:$PORT --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
